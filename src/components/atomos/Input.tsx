import { Input } from "@nextui-org/input";

type Props = {
  type: string;
};

const InputAtomo = (props: Props) => {
  return (
    <>
      <Input type={} />
    </>
  );
};

export default InputAtomo;
